name: Simple workflow
on:
  push:
    branches: main

jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
      -uses: actions/checkout@v3
      -name: run a online script
      run: echo Hi this is simple workflow
         
